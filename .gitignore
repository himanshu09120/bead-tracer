# Python
__pycache__/
*.pyc
.venv/
venv/

# Generated training/evaluation artifacts (folders are tracked via .gitkeep,
# their contents are not -- these are reproducible outputs, not source).

tensorboard/*
!tensorboard/.gitkeep


# OS
.DS_Store

# Local tooling state (not project content)
.claude/

# External cloned repo (arvijj/rl-cpp), vendored locally for benchmark
# reproduction only -- has its own git history/weights (~100MB), not
# something we should commit. Re-clone via benchmark/README.md instructions.
benchmark/baseline_rlcpp_external/official_code/
